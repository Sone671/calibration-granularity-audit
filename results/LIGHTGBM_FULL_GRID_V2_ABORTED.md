# LightGBM 完整网格 V2 预检中止记录

日期：2026-07-29。

V2 在完成 London 的 80%/1h 分支后被主动中止，未写出完整结果文件，也不进入任何分析。原因是运行中对冻结基线的逐函数对照又发现一个边界索引不一致：对 London/Ausgrid，冻结的 `full_pairs(start, stop, ...)` 使用 `start - HORIZON` 作为最早 origin，使第一目标时间戳正好落在块起点；V2 从 `start` 开始 origin，遗漏了每个块开头的一个预测跨度。

此记录不是基于性能结果选择的改动。它在 V2 的任何完整指标或结论生成前发现，且与分位数排序问题同属基线实现等价性检查。V2 日志和不完整目录保留审计，但不得使用。
